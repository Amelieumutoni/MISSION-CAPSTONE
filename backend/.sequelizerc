const path = require("path");

module.exports = {
  "config": path.resolve("src", "utils", "database", "config.js"),
  "models-path": path.resolve("src", "modules"),
  "migrations-path": path.resolve("src", "utils", "database", "migrations"),
  "seeders-path": path.resolve("src", "utils", "database", "seeders"),
};
